<%= render "shared/header" %>

<div class="main">
  <div class="container">
    
    <ul>
    <% @events.each do |event| %>      
      <li>
        <%= link_to image_tag(event.poster.url(:thumb)), event_slug(event) %>
        <%= link_to event_title(event), event_slug(event) %>
      </li>
    <% end %>
    </ul>

  </div>
</div>

<%#= render "movies" %>