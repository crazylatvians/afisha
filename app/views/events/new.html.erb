<%= render "shared/header" %>

<div class="main">
  <div class="container">

    <h1>Добавить мероприятие</h1>
    <%= form_for @event, html: { multipart: true } do |f| %>
      <%= render 'form', f: f %>
    <% end %>

  </div>
</div>